# Harbor fixture

This small static deployment viewer uses semantic HTML, plain CSS and local
JavaScript. Keep its existing visual identity: Georgia titles, system body type,
ink text, teal action accent, and a list with separators. Reuse `styles.css` tokens.
Deployment records are illustrative fixture data. No backend or network writes.
Serve with an available local static server. No install or build is needed.
