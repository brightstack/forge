# Explain an empty filtered deployment list

Create a single-page visual study for this repository's Deployments screen.
When the Production filter has no matches, help the user understand why and get
back to All environments. Show current and proposed, including a narrow-screen
view, using this app's existing design and vocabulary. Recommend a treatment.

This is a design proposal only. Write disposable artifacts and evidence under
`studies/` (and managed records under `.forge/` if useful). Keep production files
unchanged. Do not install dependencies, deploy, or publish. Use local fixture data
for any demonstrated interaction. Return a page I can open and the evidence you
actually inspected.
