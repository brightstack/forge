# Add task-specific delete labels

Update this working Todo component so every delete button exposes the accessible
name `Delete <task description>`. Preserve the visible button text and all current
add, complete/reopen, delete, and HTML-escaping behavior.

This is a small accepted Issue. Make the narrowest complete change, use only
proportionate planning and proof, and obtain independent review of the integrated
candidate. Do not add product behavior or publish the result.
