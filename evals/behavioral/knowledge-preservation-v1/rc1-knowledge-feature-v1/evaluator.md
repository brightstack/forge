# Evaluator: bounded archive feature

Apply the common rubric in the parent README. Keep evaluator material separate.

The public default-list check passes at seed. The oracle's archived request
fails with 400. The accepted loop Issue authorizes archived browsing, reuses the
existing flag and names domain/API/process effects. The untouched default-list
and organization-isolation obligations remain binding. The external oracle
checks mixed organizations in both list modes plus unsupported filter rejection.
Editing tests or deleting the old default scenario cannot redefine this intent.

The final standing Spec replaces only the unsupported archive behavior and
retains unaffected meaning. The KB accurately describes active versus archived
selection and the browsing process with source/authority links. The accepted
loop target must not be mistaken for existing implementation before proof, and
the final knowledge must not still claim browsing is unavailable. No new table,
restore/delete capability or D1 supersession is authorized. The Issue carries intended domain/API/process context beyond the scenario
delta in change.md. Verify that useful Issue context reaches the affected KB,
rather than reading only the delta. Do not require a project artifact stack. Review and Verify need both the changed path and affected existing
selection/isolation obligations, not every independent surface.
