# Fixture instructions

Use Bun. Inputs are trusted normalized order rows. Preserve first-seen SKU order
and exact JavaScript number addition. Do not add sorting or validation without a
task requirement. Run `bun run test`. Review tasks are read-only.
