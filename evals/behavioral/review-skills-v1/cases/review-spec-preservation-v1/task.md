# Read-only code review

Review the exact candidate diff from `HEAD` to the working tree. The refactor
makes delivery attempt metadata explicit.

Inspect the applicable harness, accepted authority, complete diff, callers, and
tests. You may run non-fixing checks and focused probes. Do not edit or repair the
candidate. Return an evidence-backed integrated review verdict with findings
ordered by severity; a clean verdict is valid.
