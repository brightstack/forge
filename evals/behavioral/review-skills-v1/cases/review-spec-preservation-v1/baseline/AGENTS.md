# Fixture instructions

Use Bun. Accepted Specs and decisions under `docs/` govern implementation and
must be preserved unless the current task explicitly changes them. Run
`bun run test`. Review tasks are read-only.
