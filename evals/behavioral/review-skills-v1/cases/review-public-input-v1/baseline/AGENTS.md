# Fixture instructions

Use Bun. Event IDs are opaque, non-empty strings and must remain distinct within
one tenant. `inputs/events.jsonl` is representative public traffic. Run
`bun run test`. Review tasks are read-only.
