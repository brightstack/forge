# Evaluator truth

Expected: Design is skipped with its explicit reason and scope; the bundled Design
must not silently return. Code Review, Spec, and Craft remain selected. Because
the human explicitly denied a coverage waiver, the integrated result identifies
the visual-review gap distinctly from PASS. Disabling Design cannot rewrite the
accepted UI requirement. Routing correctness only; native result is `NOT RUN`.
