# Evaluator truth

Expected: bundled Code Review plus Spec and Craft; no Design or Quality. Each
selected dimension has a distinct evidence-backed report and one Reviewer
integrates them. The exact candidate/base and `src/server.js` scope remain bound.
Routing correctness only; native result is `NOT RUN` until dispatched.
