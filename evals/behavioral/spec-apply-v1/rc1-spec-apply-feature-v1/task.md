# Apply the approved reminder-pause Spec

Use the Forge `spec apply` skill operation for the approved change in
`.forge/loops/reminder-pause/spec/issue.md`. During Spec, apply the checked
manifest at `.forge/prepared/spec-apply.json` through the CLI's existing memory
verify/apply mechanics, retrieve the resulting working-copy
knowledge for “paused reminders,” and preserve its provenance.

Complete only the approved Spec application and existing boundary fidelity check.
Then stop before Plan or Build. Report the retained accepted baseline and approved
change, receipt and result hashes, what the retrieval establishes, and which
implementation and Acceptance claims remain pending. Reuse the supplied human
authorization; ask only if its meaning conflicts with another accepted source.

Use the supplied Forge package and compiled binary. Do not edit runtime source,
start Build, commit, publish, create a branch, or create a worktree in this step.
