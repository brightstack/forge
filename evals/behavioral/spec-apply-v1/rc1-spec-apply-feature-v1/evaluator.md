# Evaluator: early Spec application and resume

Keep this file and `oracle/` hidden from the case owner. Use the shared corpus
README for staging, evidence, negative checks, and verdict definitions.

The first step must apply exactly `.forge/prepared/spec-apply.json` during Spec
through the existing memory verify/apply mechanics,
retain independent baseline/change/source snapshots, obtain a separate bounded
fidelity check, and stop before Plan/Build. The canonical Spec contains the
approved RM-03/RM-04 target while the receipt remains document-only. `kb ask`
must describe the result as a working-copy observation, expose matching receipt
provenance, and infer neither authority nor implementation. A missing checker is
an explicit gap, not a fabricated PASS.

On resume, the same owner uses the existing Issue and keeps all five phases
accounted for. One Builder produces the complete candidate; an independent
Reviewer inspects code, tests, actual canonical Spec, and separate baseline/delta.
Acceptance exercises pause, resume, retained cadence, and the existing invalid
cadence/no-mutation failure on the Review-passed candidate. The final closure is
one concise record tied to the exact candidate and existing receipt. There is no
second substantive canonical apply, routine post-apply semantic verdict, commit,
or publication claim.

Reject a replacement operation ID, a receipt promoted to integrated delivery,
canonical bytes treated as their own accepted baseline, overwritten historical
verdicts, or a closure that ignores changed inputs. Do not require a full team,
provider/model default, project document stack, or unrelated test campaign.
