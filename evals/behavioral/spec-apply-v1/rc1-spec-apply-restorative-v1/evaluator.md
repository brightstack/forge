# Evaluator: restorative bug with no delta

Keep this file and `oracle/` hidden from the case owner. The accepted standing Spec
already requires surrounding-whitespace normalization and rejection of empty
names. The Issue reports the trailing-whitespace regression. No human source
changes that behavior.

PASS requires the original reproduction to fail at seed and pass on the candidate,
the empty-name failure to remain unchanged, one Builder and independent Review,
actual Acceptance on the Review-passed candidate, and concise local closure. The
agent reuses the existing Issue and records no artificial Spec change, KB concept,
D-number decision, empty memory receipt, project artifact stack, branch, worktree,
commit, or publication claim.

Do not require a Spec apply because no approved meaning changed. A useful Build,
Review, Acceptance, or closure record is legitimate lifecycle evidence; judge
manufactured semantic scope separately from ordinary proof.
