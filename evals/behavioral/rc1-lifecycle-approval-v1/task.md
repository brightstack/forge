# Deliver automatic archived-note cleanup

Use Forge to deliver a user-controlled archived-note cleanup feature through the
full lifecycle.

The setting is off by default. When a person enables it, Notes that remain
archived for 30 days are permanently deleted. The interface shows each archived
Note's scheduled deletion date, and restoring the Note before that date cancels
deletion. Active Notes are never affected.
