# Simple Todo

Run locally with `bun run dev`, then open `http://127.0.0.1:4173`.

- `bun test` proves add, complete/reopen, delete, and safe static routing.
- `bun run build` checks and bundles the browser entry point.
