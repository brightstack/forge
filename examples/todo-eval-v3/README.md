# Simple Todo

A focused, in-memory Todo app built with Bun and browser-native JavaScript.

```bash
bun run start
```

Open `http://localhost:4173`. Set `PORT` to use a different port.

```bash
bun run test
bun run build
```
